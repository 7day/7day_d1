/*	Modulo gestione richiamo dei menu	*/exports.loginD1 = function loginD1 (d1codazie, d1coduten, d1genpass) {	var whois = require('core/User');    var ok;    var utente = [];    //debugger;    ok=whois.userLogin(d1codazie, d1coduten, d1genpass)	    if(ok.length>0)	    {       utente[0]=1;         utente[1]=ok[0].d1codazie;       utente[2]=ok[0].d1coduten;       utente[3]=ok[0].d1gentipo;              utente[4]=ok[0].d1codmatr;        utente[5]=ok[0].d1codvalu;                utente[6]=ok[0].d1codmenu;         }    else    {	        utente[0]=0; 	    }        return utente;    };exports.testdb = function testdb (d1codazie) {    var connessione = require('core/Connect');	//debugger;	if(connessione.connectTest(connessione.connectParam(d1codazie)))		return 1;	else		return 0;};exports.userSc = function userSc (utente, d1datsess) {   	var whois = require('core/User');   	debugger;   	return whois.userSessionCreate(utente[1],utente[2],d1datsess);   	};exports.userScVal = function userScVal (utente, d1datsess, progsess, time) {   	var whois = require('core/User');   	debugger;   	return whois.userSessionValidation(utente[1],utente[2],d1datsess, progsess,time);   	};exports.utenteDiProva = function utenteDiProva () {	var whois = require('core/User');	debugger;	whois.userCreate(1, "admin", "admin", "AM", 0, "VALU1", ".");};